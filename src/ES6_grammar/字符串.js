// 模板字符串
const name = "leon";
const course = "React开发App"

// 传统方式要拼接字符串我们需要用+来连接
console.log("hello"+name+',!   course is ' +course)

// ES6中我们可以简便这样写
console.log(`hello,${name}! course is ${course}`)